# Design QA — سجل تعديلات التعليقات المتتابع

## نطاق المقارنة

- مصدر الحقيقة البصري: `docs/design/comment_history/drawer_reference.png`
- لقطة التنفيذ على سطح المكتب: `docs/design/comment_history/drawer_implementation_desktop.png`
- لقطة التنفيذ على الجوال: `docs/design/comment_history/drawer_implementation_mobile.png`
- المقارنة الكاملة جنبًا إلى جنب: `docs/design/comment_history/drawer_comparison.png`
- التنفيذ المختبر: `test-results/comment-history-drawer/fixture.html` مع ملفات الحزمة الفعلية في `namar_test/public/`.

## التطبيع والحالة

- سطح المكتب: المصدر والتنفيذ كلاهما `1568 × 1003` بكسل، وحالة Drawer مفتوحة مع تعليق له تعديلان وثلاث نسخ متتابعة. حجم CSS للعرض `1568 × 1003` وكثافة الالتقاط `1x`.
- الجوال: تنفيذ `390 × 844` بكسل، وحالة Bottom Sheet مفتوحة؛ حجم CSS `390 × 844` وكثافة الالتقاط `1x`.
- المقارنة الكاملة: الصورتان المتساويتان في المقاس عُرضتا جنبًا إلى جنب داخل `drawer_comparison.png` دون اختلاف كثافة أو قص.
- الصفحة المحيطة في التنفيذ المحلي Fixture مبسطة؛ المقارنة الوظيفية والبصرية تركز على زر السجل والـDrawer والبطاقات، مع إبقاء ألوان وتوكنات Frappe الفعلية.

## النتائج

لا توجد فروقات P0 أو P1 أو P2 قابلة للتنفيذ بعد الجولة النهائية.

- الخطوط والتسلسل: استخدم التنفيذ خط النظام الخاص بـFrappe وأحجام `--text-sm` و`--text-xs` بدل تكبير خط الصورة التصورية. بقي التسلسل واضحًا: عنوان، هوية ووقت، ووسم النسخة، ثم المحتوى. هذا اختلاف مقصود للحفاظ على نظام Frappe البصري وليس انحرافًا وظيفيًا.
- المسافات والتخطيط: عرض اللوحة `clamp(420px, 40vw, 640px)` يطابق نسبة 40% في المصدر (`627px` عند عرض `1568px`). تبدأ أسفل شريط النظام عند `48px`، والبطاقات مرتبة على خط زمني رأسي. على الجوال تتحول إلى Bottom Sheet بعرض كامل وارتفاع `82dvh` مع safe-area padding.
- الألوان والتوكنات: استخدمت ألوان Frappe الدلالية للحالية والحدود والخلفيات والتعتيم. لا توجد تدرجات أو ألوان مستقلة عن النظام، والتباين المرئي مناسب.
- الصور والأصول: الصورة المرجعية توضيحية فقط. التنفيذ يستخدم Avatar وIcon الحقيقيين من Frappe؛ لم تُنشأ SVG أو أيقونات أو رسوم CSS بديلة داخل كود المنتج.
- النص والمحتوى: أزيل عرض «قبل التعديل/بعد التعديل». يظهر «تم التعديل مرتين • عرض السجل»، ثم «النسخة الحالية»، «النسخة 2 من 3»، و«أقدم نسخة مسجلة» مع المؤلف والوقت الصحيحين.
- الوصول والحركة: زر حقيقي مع `aria-haspopup`, `aria-controls`, و`aria-expanded`؛ Drawer بدور `dialog` و`aria-modal`; القائمة `ol/li/article`; الحالية تحمل `aria-current`; Escape وBackdrop والإغلاق يعملون؛ Tab محصور داخل اللوحة؛ التركيز يعود للزر؛ والحركة تحترم `prefers-reduced-motion`.

## المقارنة المركزة

- فُتحت لقطة المصدر ولقطة التنفيذ منفردتين بالحجم الأصلي بعد المقارنة الكاملة لتدقيق عنوان اللوحة، موضع الإغلاق، البطاقات الثلاث، شريط النسخ، خط الزمن، والمسافات.
- لقطة الجوال `drawer_implementation_mobile.png` كانت مقارنة مركزة للاستجابة والتفاف الأسماء والتواريخ. لا يلزم قص إضافي لأن كل تفاصيل المكون ظاهرة ومقروءة في اللقطتين الأصليتين.

## سجل جولات المقارنة

1. أظهر فحص الجوال الأول التفاف تاريخ أقدم نسخة داخل الشرطة الوسطية، وهو فرق P2 في قابلية القراءة.
2. عُدّل الـmeta إلى وحدات دلالية مستقلة، مع عنصر `time` غير قابل للكسر و`datetime` بصيغة صحيحة، ثم أُعيد الالتقاط.
3. اللقطة النهائية `drawer_implementation_mobile.png` تُظهر التفافًا منظمًا بين «صاحب التعليق» والتاريخ من دون كسر التاريخ، ولم يبق فرق P0/P1/P2.

## التفاعلات والاختبارات

- سطح المكتب: زر واحد، Drawer واحد، ثلاث بطاقات بالترتيب الصحيح، عرض `627px` واتجاه RTL.
- الجوال: Bottom Sheet عند `390 × 844`، هندسته `390 × 692` ومثبتة أسفل الشاشة.
- لوحة المفاتيح: التركيز يبدأ على زر الإغلاق، وTab وShift+Tab محصوران، وEscape يغلق ويعيد التركيز للزر.
- إعادة الرسم أثناء فتح Drawer: تُغلق اللوحة بالكامل، يُزال قفل التمرير، يبقى Overlay واحد وTrigger واحد، ولا يبقى التركيز داخل سطح مخفي.
- Console: لا أخطاء ولا تحذيرات مرتبطة بالمكون في سيناريوهات سطح المكتب والجوال وإعادة الرسم.
- الاختبارات الساكنة: Node syntax، Deferred وPromise، cache/inflight والرد القديم، snapshots، واختبارات Python وRTL/CSS ناجحة.

## متابعة اختيارية P3

- يمكن تقليل كثافة النص أو زيادة فراغ البطاقات لاحقًا إذا فضّل المستخدم مظهرًا أقرب حرفيًا للصورة التصورية، لكن الوضع الحالي أنسب لتوكنات Frappe ومساحة Desk الفعلية.

final result: passed
