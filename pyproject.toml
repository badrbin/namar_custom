[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "namar_test"
version = "0.0.6"
description = "Namar ERPNext customizations migrated from live test scripts, including delivery component tracking."
requires-python = ">=3.10"
authors = [{ name = "Namar", email = "badrarroug@namar.net" }]
dependencies = []
