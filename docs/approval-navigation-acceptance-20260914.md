# قبول فتح الموافقة في تبويب مستقل — TEST

اجتاز التغيير اختبار النقر الحي بتاريخ 14 سبتمبر 2026 عند 07:13:30 بتوقيت الرياض.

- النسخة المنشورة: `00afa68b27f610ec7c46857ab3f9dda2b3565073`.
- Pipeline النشر: `7rbkimhha8`.
- تطابق JavaScript الحي مع المصدر عند 07:11:06، بعد إزالة لاحقة sourceURL
  القياسية فقط.
- فُتح المستند في تبويب جديد، وتحمل عنوانه، وبقيت صفحة الموافقات الأصلية
  والموافقة المختارة والعداد 3,246 دون تغيير.
- لم ينفذ الاختبار اعتمادًا أو تعديل بيانات أعمال.
- بقي محرك الفهرس ready في الجيل 40 وبصمته السابقة؛ لم يتغير الكود الخلفي
  أو قواعد الصلاحيات ولم تتطلب إضافة التنقل إعادة بناء الفهرس.

التغيير يستخدم helper فتح المستند الموجود بدل `frappe.set_route`، ويغطي
المستند المرتبط وبديل Workflow Action. نجحت اختبارات التنقل وعدادات المصادر
وحالات الفهرس وsyntax في مساحتي اسم TEST وPROD. وتغطي الاختبارات الروابط
الآمنة و`noopener,noreferrer` وعدم تغيير الصفحة الأصلية أو تنفيذ استدعاء كتابة.

هذا السجل يثبت قبول TEST فقط. الإيصال التفصيلي محفوظ محليًا بصلاحية خاصة؛
لا يتضمن هذا الملف حسابات أو أسماء عملاء أو معرّفات مستندات أعمال أو أسرارًا.
