# إيقاف توجيه الموافقات المخصص — 13 سبتمبر 2026

## طلب المستخدم ونطاق الاستجابة

طلب المستخدم إيقاف الميزة عاجلًا بعد ظهور بطء شديد وInternal Server Error على الأساسي، بعد تعيين «منشئ الطلب» لحالة «مكتمل» في Workflow «طلب مواد».

أُلغي مسار نشر التحسينات التجريبية الجاري. الاستجابة الإنتاجية محصورة في إيقاف **توجيه مستلمي الموافقات المخصص**، وليست إيقاف الوارد أو المتابعات أو صلاحيات الاعتماد القياسية.

## التغيير المحدد

- أساس الإنتاج: `095905956f4be1116b9b48688e7cd9caae4b04de`.
- الإصلاح الطارئ: `106694dc97489682ad815d3e3fbf221adeb69e4b`، ثلاثة أسطر في `approval_routing.py`، دون نقل أي تحسين آخر.
- إعداد الموقع: `disable_followup_approval_routing = 1` بنوع **Boolean**.
- عند التفعيل، يتوقف تحميل قواعد المستلمين ومراجعها، وتعمل الموافقات وفق أدوار Workflow Action القياسية.
- تبقى JSON إعدادات المستلمين والطلبات والحالات والانتقالات محفوظة كما هي.
- لا تُستخدم القيمة النصية `"false"` للإلغاء؛ القيم النصية غير الفارغة تعد مفعلة في Python.

## التطبيق العاجل والتحقق

- إعداد الموقع طُبّق بمهمة Cloud `459ab3jooa` بنجاح.
- App Patch على التطبيق `namar_custom` والـBench الإنتاجي `bench-13170-000235-f1uae` فقط: المهمة `c675dcg3eo` نجحت في 09:29 بتوقيت الرياض، وشملت إعادة تشغيل Bench.
- قبل الإيقاف، انتهت مهلة طلب هوية الجلسة البسيط بعد 20 ثانية.
- بعد الإيقاف، نجح فحص root: الهوية 0.187 ثانية، العدادات 0.330 ثانية، وقائمة 25 موافقة 0.301 ثانية، بلا رسائل خطأ من السيرفر.
- تحقق مستقل في 09:34: ثلاث عينات للعدادات بين 0.372 و0.494 ثانية، وللقائمة بين 0.301 و0.410 ثانية؛ كلها HTTP 200، وRouting = Role.
- طلبات Guest للعدادات والقائمة رُفضت بـ403 دون بيانات.
- Workflow «طلب مواد» ظل مطابقًا كاملًا للنسخة القبلية، بما فيه modified وجميع JSON المستلمين: 46 حالة و62 انتقالًا. لم يُنفّذ حفظ Workflow أثناء الإيقاف.

الأدلة المفصلة محفوظة محليًا بصلاحيات خاصة ضمن `outputs/approval_visibility_performance_20260913/`؛ لا تُرفع النسخ الخاصة أو الأسرار إلى Git.

## التثبيت الدائم

رُفع الإصلاح الطارئ وحده إلى فرع `production`. بدأ تثبيته عبر Pipeline `1a7nvp1mfn` في 09:35، مع اختيار Namar Custom فقط؛ بقي Namar Test وباقي التطبيقات دون تحديث. يجب إضافة نتيجة التثبيت والتحقق اللاحق عند اكتماله، ولا يكفي نجاح App Patch وحده لإثبات الديمومة.

## الاستعادة والسياسة اللاحقة

تبقى الميزة المخصصة متوقفة. لا تُعاد تلقائيًا بسبب انتهاء اختبارات التجريبي أو توفر فرع أحدث. إعادة تفعيلها تحتاج طلبًا جديدًا من المستخدم ومعالجة حمل القواعد ذات الأعداد الكبيرة والتحقق منه أولًا.

الرجوع البرمجي مرجعه commit الإصلاح أعلاه. حفظ قيمة المفتاح false أو حذفه يعيد التوجيه المخصص الموجود؛ لذلك لا يُنفذ ذلك أثناء هذه الحادثة. لا حاجة إلى استعادة قاعدة بيانات أو حذف مهام أو تغيير أدوار المستخدمين.
